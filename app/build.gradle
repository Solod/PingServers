apply plugin: 'com.android.application'
apply plugin: 'android-apt'

android {
    signingConfigs {
        app_dep {
            keyAlias 'my_key'
            keyPassword 'ru0afsbfdi'
            storeFile file('/home/jack/Android/my_key.jks')
            storePassword 'ru0afsbfdi'
        }
    }
    compileSdkVersion 24
    buildToolsVersion "23.0.3"
    defaultConfig {
        applicationId "com.solodilov.evgen.pingservers"
        minSdkVersion 16
        targetSdkVersion 24
        versionCode 3
        versionName "0.1-alfa"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.app_dep
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:24.0.0'
    compile 'com.android.support:support-v4:24.0.0'
    compile 'com.android.support:preference-v7:24.0.0'
    compile 'com.jakewharton:butterknife:8.0.1'
    apt 'com.jakewharton:butterknife-compiler:8.0.1'
}
